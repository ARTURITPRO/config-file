server:
  port: 8080

spring:
  datasource:
    url: ${POSTGRES_DB:jdbc:postgresql://192.168.99.19:9094/reviewapp}
    username: ${POSTGRES_USER:reviewapp-admin}
    password: ${POSTGRES_PASSWORD:jk8LS!sAx5}

logging:
  level:
    root: INFO